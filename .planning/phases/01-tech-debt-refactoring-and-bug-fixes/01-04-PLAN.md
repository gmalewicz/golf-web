---
phase: 01-tech-debt-refactoring-and-bug-fixes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/style.scss
  - src/styles.css
  - angular.json
  - src/app/scorecard/_helpers/golfRxStompConfig.ts
  - src/app/_models/player.ts
  - src/environments/environment.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Only one global stylesheet file exists (style.scss)"
    - "styles.css is deleted and removed from angular.json"
    - "All global CSS classes from styles.css are present in style.scss"
    - "No commented-out code blocks remain in identified files"
    - "Application builds and looks identical to before"
  artifacts:
    - path: "src/style.scss"
      provides: "Consolidated global styles (Material theme + utility classes)"
      contains: ".golf-green"
    - path: "angular.json"
      provides: "Updated styles array without styles.css"
  key_links:
    - from: "angular.json styles array"
      to: "src/style.scss"
      via: "build configuration"
      pattern: "src/style.scss"
---

<objective>
Clean up code debt: consolidate dual CSS/SCSS global stylesheets into one file, and remove commented-out dead code from identified source files.

Purpose: Eliminates confusion from having two global style files and removes code noise from commented-out blocks.
Output: Single `style.scss` with all global styles, clean source files without commented-out code.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tech-debt-refactoring-and-bug-fixes/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate styles.css into style.scss and remove the duplicate</name>
  <files>
    src/style.scss
    src/styles.css
    angular.json
  </files>
  <read_first>
    - src/style.scss
    - src/styles.css
    - angular.json
  </read_first>
  <action>
Currently both `src/styles.css` and `src/style.scss` are loaded in `angular.json` styles array. They contain:

- `src/style.scss`: Angular Material theming (palette definition, `mat.all-component-typographies()`, `mat.elevation-classes()`, `mat.app-background()`)
- `src/styles.css`: Global utility CSS classes (`.golf-green`, `.blackDot`, `.redDot`, `.highlight`, `.centerImg`, etc. — 140 lines of utility styles)

**Steps:**

1. **Read both files fully** to understand all content.

2. **Append all CSS from `src/styles.css` to the end of `src/style.scss`**, after the existing Material theme SCSS content. Add a comment separator:
   ```scss
   // === Global utility styles (consolidated from styles.css) ===
   ```
   Then paste all the CSS classes from `styles.css` below. SCSS is a superset of CSS, so the plain CSS will work directly in the `.scss` file.

3. **Update `angular.json`** — remove `"src/styles.css"` from the `styles` array in BOTH the `build` and `test` configurations:
   - In `projects.golf-web.architect.build.options.styles`: Remove the `"src/styles.css"` entry (keep `style.scss`, the Material prebuilt theme, Bootstrap, and primeicons)
   - In `projects.golf-web.architect.test.options.styles` (around line 124): Remove `"src/styles.css"` if present

4. **Delete `src/styles.css`** — it is now fully consolidated into `style.scss`.

5. **Verify:** Run `ng build` — the build must succeed and all styles must still apply.
  </action>
  <acceptance_criteria>
    - `src/styles.css` does not exist: `Test-Path src/styles.css` returns False
    - `src/style.scss` contains `.golf-green` class (from former styles.css)
    - `src/style.scss` contains `mat.all-component-typographies` (original SCSS content preserved)
    - `angular.json` does not contain the string `styles.css`
    - `ng build` succeeds
    - `ng test --watch=false` passes all tests
  </acceptance_criteria>
  <verify>
    <automated>ng build</automated>
  </verify>
  <done>Single global stylesheet. styles.css deleted. angular.json updated. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Remove commented-out dead code from identified files</name>
  <files>
    src/app/scorecard/_helpers/golfRxStompConfig.ts
    src/app/_models/player.ts
    src/environments/environment.ts
  </files>
  <read_first>
    - src/app/scorecard/_helpers/golfRxStompConfig.ts
    - src/app/_models/player.ts
    - src/environments/environment.ts
  </read_first>
  <action>
Remove commented-out code blocks identified in CONCERNS.md:

1. **`src/app/scorecard/_helpers/golfRxStompConfig.ts`**: Remove commented-out STOMP debug logging lines. These are `// debug:` configuration lines that were disabled. Remove the entire commented block, not just individual lines.

2. **`src/app/_models/player.ts`**: Remove commented-out `token` and `refreshToken` fields. These were part of the Player interface/class but were commented out. Remove them entirely — JWT is stored in localStorage via AuthenticationService, not in the Player model.

3. **`src/environments/environment.ts`**: Remove commented-out `WS_ENDPOINT` line. The WebSocket endpoint is loaded at runtime from `assets/app-config.json`, not from environment config.

**Rules:**
- Only remove clearly commented-out CODE (not explanatory comments)
- Do NOT remove `// NOSONAR` comments (these are intentional SonarCloud suppressions)
- Do NOT remove the `// TODO` comment in `style.scss` about Material v15 typography (that's informational, but actually we should remove it since it's outdated — the project is on Angular 21/Material 21, not v15. Remove the TODO comment block at the top of style.scss as well.)
- Verify each removal doesn't break functionality by reading the surrounding code

After edits, run `ng test --watch=false` to confirm no regressions.
  </action>
  <acceptance_criteria>
    - `golfRxStompConfig.ts` contains no commented-out `debug:` lines
    - `player.ts` contains no commented-out `token` or `refreshToken` fields
    - `environment.ts` contains no commented-out `WS_ENDPOINT` line
    - `style.scss` contains no outdated `TODO(v15)` comment block
    - `ng test --watch=false` passes all tests
    - `ng build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>ng test --watch=false</automated>
  </verify>
  <done>All identified commented-out code removed. Source files are clean without dead code noise.</done>
</task>

</tasks>

<verification>
1. `ng test --watch=false` — all tests pass
2. `ng build` — build succeeds
3. `Test-Path src/styles.css` — returns False
4. No commented-out code blocks in the 3 identified files
</verification>

<success_criteria>
- Single global stylesheet (style.scss only)
- styles.css deleted, angular.json updated
- No commented-out dead code in golfRxStompConfig.ts, player.ts, environment.ts
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-tech-debt-refactoring-and-bug-fixes/01-04-SUMMARY.md`
</output>
