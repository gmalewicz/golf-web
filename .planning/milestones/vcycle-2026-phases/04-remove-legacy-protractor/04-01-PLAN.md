---
phase: 04-remove-legacy-protractor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [angular.json, README.md]
autonomous: true
requirements: [PROTRACTOR-01]

must_haves:
  truths:
    - "No Protractor references remain in project configuration"
    - "ng e2e no longer references a dead Protractor config"
    - "README accurately describes Cypress as the E2E solution"
  artifacts:
    - path: "angular.json"
      provides: "Project configuration without Protractor e2e target"
      contains: "No protractor builder reference"
    - path: "README.md"
      provides: "Accurate E2E documentation pointing to Cypress"
      contains: "cypress"
  key_links:
    - from: "angular.json"
      to: "cypress/"
      via: "E2E testing is handled by Cypress only"
      pattern: "No protractor pattern should exist"
---

<objective>
Remove all legacy Protractor E2E references from the project configuration and documentation.

Purpose: The `e2e/` directory with Protractor tests has already been deleted, but `angular.json` still contains a dead `"e2e"` architect target pointing to non-existent `e2e/protractor.conf.js`, and `README.md` still references Protractor. Cypress at `cypress/` is the active E2E solution.

Output: Clean `angular.json` without dead Protractor target; updated README referencing Cypress.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@angular.json
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Protractor e2e target from angular.json and update README</name>
  <files>angular.json, README.md</files>
  <action>
1. In `angular.json`, delete the entire `"e2e"` architect target block (lines ~129-140) — the object under `projects.golf-web.architect.e2e` that uses `@angular-devkit/build-angular:protractor` and references `e2e/protractor.conf.js`. Keep all other targets (`build`, `serve`, `test`, `deploy`, `lint`) intact.

2. In `README.md`, replace the "Running end-to-end tests" section (lines ~22-23):
   - OLD: `Run \`ng e2e\` to execute the end-to-end tests via [Protractor](http://www.protractortest.org/).`
   - NEW: `Run \`npx cypress open\` to execute the end-to-end tests via [Cypress](https://www.cypress.io/).`
  </action>
  <verify>
    <automated>node -e "const aj=require('./angular.json'); const targets=Object.keys(aj.projects['golf-web'].architect); if(targets.includes('e2e')){process.exit(1)} console.log('angular.json OK: no e2e target'); const fs=require('fs'); const readme=fs.readFileSync('README.md','utf8'); if(readme.includes('Protractor')){process.exit(1)} if(!readme.includes('Cypress')){process.exit(1)} console.log('README.md OK: references Cypress, no Protractor')"</automated>
  </verify>
  <done>angular.json has no Protractor e2e target; README references Cypress for E2E tests; no Protractor mentions remain in project config or docs.</done>
</task>

</tasks>

<verification>
- `angular.json` parses as valid JSON
- No `protractor` string in `angular.json`
- No `Protractor` string in `README.md`
- `README.md` mentions Cypress for E2E
- `ng build` still succeeds (e2e target removal doesn't affect build)
</verification>

<success_criteria>
All legacy Protractor references removed from project configuration and documentation. Cypress documented as the E2E testing solution.
</success_criteria>

<output>
After completion, create `.planning/phases/04-remove-legacy-protractor/04-01-SUMMARY.md`
</output>
